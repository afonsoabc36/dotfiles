[ids]

*

[main]

# Maps capslock to escape when pressed and control when held.
capslock = overload(control, esc)
# Maps shift to backspace when pressed and shift when held.
# shift = overload(shift, backspace)

# Remaps the escape key to capslock
esc = capslock

[alt]
q = macro(S-`)

[altgr]
q = macro(S-`)
